const assert = require('assert');

describe('file to test', (params) => {
    context('function to test',(params) => {
        it('should do this specific thing', (params) => {
            assert.equal(1,1);
        });

        it('will be written later');
    });
});
